services:
  docs:
    image: collateral_ai_local_docs
    container_name: collateral_ai_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./collateral_ai:/app/collateral_ai:z
    ports:
      - '9000:9000'
    command: /start-docs
